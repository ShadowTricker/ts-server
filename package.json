{
  "name": "ts-server",
  "version": "1.0.0",
  "description": "node server of typescript",
  "main": "index.js",
  "scripts": {
    "start": "supervisor -w ./src -e ts,js -x ts-node ./src/index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shadow tricker",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.10"
  }
}
